import 'package:flutter/material.dart';

class VorschlaegeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Ihre Vorschläge"),
        ),
        body: Container());
  }
}
