import 'package:flutter/material.dart';

class WunschlisteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Unsere Wunschliste"),
        ),
        body: Container());
  }
}
