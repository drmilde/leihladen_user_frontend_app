import 'package:flutter/material.dart';

class LeihausweisScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Leihausweis"),
      ),
      body: Container(),
    );
  }
}
