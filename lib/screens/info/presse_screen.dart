import 'package:flutter/material.dart';

class PresseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Pressebericht 2019"),
        ),
        body: Container());
  }
}
